import { TopicDifficultyPipe } from './topic-difficulty.pipe';

describe('TopicDifficultyPipe', () => {
  it('create an instance', () => {
    const pipe = new TopicDifficultyPipe();
    expect(pipe).toBeTruthy();
  });
});
